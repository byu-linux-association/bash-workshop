#!/bin/bash

# This task is to iterate over everything in a directory and print out its name.
# This script should be very similar to the `ls` command.
#
# Hint: use file globbing to get everything in a directory.
# Also, don't use `ls` command anywhere in your code
